import React from "react";

export const CartHeader = (props) => {

    return (
        <div className="cart-name-head">
            <h1>
                "Shopping Cart"
            </h1>
        </div>
    );
};

